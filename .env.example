# Absolute URL of the mnemonic MCP endpoint (streamable HTTP, path /mcp).
# Defaults to a same-origin "/mcp" if unset — only works if this app is
# reverse-proxied from the same hostname as the mnemonic origin.
VITE_MCP_URL=https://mnemonic.dowling.nz/mcp
